# Progress

Агенты дополняют этот файл кратким summary после завершения задач из `tasks.json`.

---

## Верстка: `edit-space` и `SearchBar` (company-space)

**Дата:** 2026-04-16

Задачи из `tasks.json` не закрывались; правки UX/вёрстки.

### `src/app/edit-space/`

- **Кнопки «Отмена» / «Сохранить»:** обёртка `.adminColumn` (flex-колонка, `gap: 24px` по макету Figma `763:5636`), чтобы кнопки шли сразу под карточкой «Данные администратора», а не во второй строке grid под высокой левой карточкой с календарём.
- **Футер в зоне видимости:** у `.layout` убраны `min-height: 100vh` (дублировали высоту вместе с глобальным `Header` 93px). Заданы `height` / `max-height: calc(100dvh - 93px)` и `overflow: hidden`; прокрутка длинного контента в `.main` (`flex: 1`, `min-height: 0`, `overflow-y: auto`).

### `src/shared/components/SearchBar/`

- У `.header`, `.filtersRow`, `.bottomRow` снят `max-width: 1360px`, чтобы строки занимали всю ширину между горизонтальными отступами (`40px`, как у шапки). Поиск, кнопка действия и фильтры прижаты к правому краю контентной области.

### Затронутые страницы

- `/company-space`, `/edit-data` (тот же `SearchBar`), `/edit-space`.

---

## Синхронизация статусов с кодом (git)

**Дата:** 2026-04-10

По текущим изменениям в репозитории (BFF auth, proxy, `RestProvider`, `AuthGuard`, логин, `forbidden`) обновлены статусы в `docs/tasks.json`.

### TASK-001 — done (с оговорками)

- Есть `RestProvider` + `restProviderInstance` с `NEXT_PUBLIC_API_URL` из `getPublicEnv()`, BFF-прокси `/api/proxy/*`.
- HTTP-ошибки (`!res.ok`) маппятся в `RestProvider.request`.
- **Осталось довести до полного AC:** обёртка **сетевых** сбоев/таймаутов в одном месте; в `CourseCard` по-прежнему сырой `fetch` — вынести на клиент или задокументировать исключение.

### TASK-002 — done

- Сессия: httpOnly-cookies через `/api/auth/login`, refresh в `/api/auth/session` и прокси, состояние пользователя в `AuthStore`, повторная гидратация после F5.
- Выход: `/api/auth/logout`, сброс `session` в сторе, редирект на логин из `Header`.
- Токены на клиенте в открытом виде не хранятся для API-вызовов (cookie + server BFF).

### TASK-003 — pending

- В прокси есть реакция на 401 (очистка cookies, JSON).
- Нет централизованной обработки **приостановки пространства** по контракту API и полного сценария 403/блокирующего UI из описания задачи.

### TASK-004 — done

- `AuthGuard`: публичные маршруты, редирект без сессии на логин, проверка ролей `canAccessRoute` для префиксов `/statistics`, `/candidates`, `/courses`.
- Страница `/forbidden` при несовпадении роли.

### TASK-005 — done (с оговоркой)

- `LoginStore` + `LoginForm`: валидация email/пароля, вызов `auth.login`, сообщения об ошибках с BFF.
- Успешный вход → редирект с публичного маршрута на home через `AuthGuard`.
- Модалки «забыли пароль» без интеграции с API (это **TASK-007/008**, не закрыты).
- **Test step 3** (приостановленное пространство) зависит от API и **TASK-003** — при появлении контракта проверить отдельно.

### Не затронуто этим набором файлов

- TASK-006 и далее по списку — без изменений статуса (реализация не подтверждена текущим diff).

---

## Обновление контекста после RBAC и auth-hardening

**Дата:** 2026-04-13

### Что добавлено

- Введён централизованный RBAC-конфиг: `src/shared/config/roles.config.ts`.
  - `getAvailableNav(role)` для фильтрации navbar.
  - `canAccessRoute(role, pathname)` для route guard.
  - Обработка неизвестной роли как `guest`.
- Обновлён middleware (`src/middleware.ts`):
  - Проверка валидности сессии через вызов `/api/auth/session` перед доступом к закрытым страницам.
  - Редирект неавторизованных на `/login` с `next`.
  - Обработка `workspace_suspended` с редиректом на `/subscription-paused`.
- Обновлены клиентские guards:
  - `AuthGuard` использует RBAC-правила и fallback-редиректы.
  - Устранён визуальный блик защищённого контента во время редиректа.
- Обновлён `Header`:
  - Пункты меню теперь рендерятся только по доступным правам роли.
  - Для `candidate` и `guest` меню не отображается.
- Добавлены/обновлены страницы:
  - `src/app/users/page.tsx` (раздел пользователей, заглушка).
  - `src/app/subscription-paused/page.tsx` (блокирующий экран).

### Где остановились

- По `tasks.json` актуально: `TASK-004` остаётся `done`, `TASK-003` остаётся `pending`.
- Логика приостановки пространства добавлена технически, но требуется финальная сверка с backend-контрактом (какое поле/статус является источником истины).
- Требуется финальное согласование продуктовой матрицы для `superadmin` vs `admin` (платформенный или tenant-суперадмин) и фиксация этого решения в одном варианте без дальнейших колебаний.
- После согласования — прогон ручных сценариев: прямой URL, переходы из navbar, 401/expired session, suspended workspace.

---

## Доработки auth/RBAC после ручной проверки ролей

**Дата:** 2026-04-13

### Что исправлено

- `AuthGuard`:
  - Исправлена реактивность редирект-эффекта (зависимости по `isHydrated/isAuthorized/canAccessRoute`), устранено зависание на `Загрузка...`.
- `forbidden`:
  - `PAGES.FORBIDDEN` добавлен в публичные маршруты (`PUBLIC_ROUTE_PATHNAMES`), убран цикл редиректа `forbidden -> forbidden`.
- Нормализация ролей backend:
  - Поддержаны фактические форматы: `SUPER ADMIN`, `COMPANY ADMIN`, `COMPANY USER`, `CANDIDATE`, `UNREG`.
  - Маппинг в внутренние роли приложения: `superadmin/admin/user/candidate/unrecognized`.
- `Header` / меню:
  - Для `candidate` включён доступный пункт навигации `courses` (если у роли есть хотя бы один разрешённый роут, он отображается в шапке).

### Статусы задач

- `TASK-004` остаётся `done`: роль и RBAC-поток доведены до рабочего состояния с учётом фактических backend-ролей.
- `TASK-003` остаётся `pending`: требуется финальный целевой прогон `test_steps` по 401/suspended на полном сценарии и фиксация результата.

### Уточнение формулировок задач

- В `docs/tasks.json` обновлены описания `TASK-002..TASK-005` под текущую архитектуру:
  - BFF + httpOnly cookies для сессии;
  - централизованная обработка 401/403/paused;
  - нормализация backend-ролей и устранение циклических редиректов;
  - role-based post-login редиректы.
