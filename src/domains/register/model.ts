export type RegistrationFormValues = {
  name: string;
  password: string;
  confirmPassword: string;
};

export type RegistrationFormErrors = {
  name: string;
  password: string;
  confirmPassword: string;
};
